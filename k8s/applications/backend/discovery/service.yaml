apiVersion: v1
kind: Service
metadata:
  name: discovery
  namespace: egov-msa
  labels:
    env: production
    tier: backend
    app: discovery
    name: discovery-service
spec:
  type: NodePort
  ports:
    - port: 8761
      targetPort: 8761
      nodePort: 30102
  selector:
    env: production
    tier: backend
    app: discovery
    name: discovery-pod