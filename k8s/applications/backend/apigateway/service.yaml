apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: egov-msa
  labels:
    env: production
    tier: backend
    app: apigateway
    name: apigateway-service
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30101
  selector:
    env: production
    tier: backend
    app: apigateway
    name: apigateway-pod