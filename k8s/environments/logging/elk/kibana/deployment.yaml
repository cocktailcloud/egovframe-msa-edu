apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana-deployment
  namespace: egov-msa
  labels:
    env: production
    tier: backend
    app: kibana
    name: kibana-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      env: production
      tier: backend
      app: kibana
      name: kibana-pod
  template:
    metadata:
      labels:
        env: production
        tier: backend
        app: kibana
        name: kibana-pod
    spec:
      containers:
      - name: kibana
        image: docker.elastic.co/kibana/kibana:7.10.1
        env:
          - name: ELASTICSEARCH_URL
            value: http://elasticsearch.egov-msa.svc.cluster.local:9200
          - name: ELASTICSEARCH_HOSTS
            value: '["http://elasticsearch.egov-msa.svc.cluster.local:9200"]'
          - name: TZ
            valueFrom:
              configMapKeyRef:
                name: common-configmap
                key: TZ
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: 1
            memory: 1Gi
        ports:
        - containerPort: 5601
          name: http
        volumeMounts:
          - name: kibana-config-volumne
            mountPath: /usr/share/kibana/config/
      volumes:
      - name: kibana-config-volumne
        configMap:
          name: kibana-configmap
